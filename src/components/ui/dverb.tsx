/**
 * Verb chip. The verb-action label (e.g. "PR Present") is no longer shown in the UI,
 * so this renders nothing. The underlying verb metadata/logic is untouched.
 */
export function DVerb(_props: { verbId: string }) {
  return null;
}
