"use client";

import { ReviewSurface } from "@/components/mentor/review-surface";

/** One delivery. Everything it needs comes from the URL and the layouts' providers. */
export default function MenteeStepReviewPage() {
  return <ReviewSurface />;
}
