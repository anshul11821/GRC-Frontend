"use client";

import { TaskOverview } from "@/components/app/task-overview";

/** The learner's task brief. The body is shared with the mentor's Review Desk — see the component. */
export default function TaskOverviewPage() {
  return <TaskOverview />;
}
